#ifndef QUEUE_H
#define QUEUE_H

#include <stdbool.h>

// TODO: Define SimpleQ with required attributes
typedef struct {} SimpleQ;

// Declaration of the basic functions to be tested
void init_queue(SimpleQ* _sQueue);
bool is_empty(SimpleQ* _sQueue);

#endif /* QUEUE_H */